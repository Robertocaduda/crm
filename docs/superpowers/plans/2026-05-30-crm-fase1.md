# CRM Fase 1 — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Configurar o monorepo CRM com autenticação JWT e dashboard mockado, pronto para receber os módulos das fases seguintes.

**Architecture:** Monorepo Turborepo com `apps/web` (Next.js 14 App Router) e `apps/api` (Express + Node.js), tipos compartilhados via `packages/shared`, Prisma Client em `packages/db`. O Next.js faz proxy de todas as requisições `/api/*` para o Express, garantindo que os cookies httpOnly fiquem num único origem (localhost:3000).

**Tech Stack:** Next.js 14, Express 4, PostgreSQL 16 (Docker), Prisma 5, Tailwind CSS 3, JWT (jsonwebtoken), bcrypt, Turborepo 2, tsx

---

## Mapa de Arquivos

```
crm/
├── apps/
│   ├── api/
│   │   ├── src/
│   │   │   ├── index.ts                     # entry point (listen)
│   │   │   ├── app.ts                       # express config + middleware + rotas
│   │   │   ├── lib/
│   │   │   │   ├── jwt.ts                   # sign/verify access + refresh tokens
│   │   │   │   └── cookie.ts                # set/clear auth cookies
│   │   │   ├── middleware/
│   │   │   │   └── authenticate.ts          # verifica access_token no cookie
│   │   │   ├── controllers/
│   │   │   │   └── auth.controller.ts       # login, logout, refresh, me
│   │   │   └── routes/
│   │   │       └── auth.ts                  # router /api/auth/*
│   │   ├── .env                             # variáveis locais da API
│   │   ├── package.json
│   │   └── tsconfig.json
│   └── web/
│       ├── app/
│       │   ├── layout.tsx                   # root layout (html/body + globals.css)
│       │   ├── globals.css                  # tailwind directives
│       │   ├── (auth)/
│       │   │   └── login/
│       │   │       └── page.tsx             # página de login (client component)
│       │   └── (protected)/
│       │       ├── layout.tsx               # layout protegido: busca /me, renderiza sidebar
│       │       └── dashboard/
│       │           └── page.tsx             # página do dashboard
│       ├── components/
│       │   ├── sidebar.tsx                  # sidebar de navegação
│       │   ├── topbar.tsx                   # barra superior
│       │   └── dashboard/
│       │       ├── kpi-card.tsx             # card de KPI
│       │       ├── funnel-chart.tsx         # funil de vendas
│       │       ├── activity-feed.tsx        # atividades recentes
│       │       ├── deals-list.tsx           # negociações em destaque
│       │       └── upcoming-tasks.tsx       # próximas atividades
│       ├── lib/
│       │   ├── api-client.ts                # fetch wrapper (client-side)
│       │   └── mock-data.ts                 # dados mockados do dashboard
│       ├── middleware.ts                    # proteção de rotas Next.js
│       ├── next.config.ts
│       ├── tailwind.config.ts
│       ├── postcss.config.js
│       ├── .env.local
│       ├── package.json
│       └── tsconfig.json
├── packages/
│   ├── shared/
│   │   ├── src/
│   │   │   ├── index.ts                     # re-exports
│   │   │   └── types/
│   │   │       ├── user.ts                  # User interface, Role enum
│   │   │       └── auth.ts                  # LoginRequest, MeResponse
│   │   ├── package.json
│   │   └── tsconfig.json
│   └── db/
│       ├── prisma/
│       │   ├── schema.prisma                # schema User
│       │   └── seed.ts                      # cria admin@crm.dev
│       ├── src/
│       │   └── index.ts                     # exporta prisma client
│       ├── package.json
│       └── tsconfig.json
├── .env.example
├── .gitignore
├── docker-compose.yml
├── package.json                             # workspace root + scripts
└── turbo.json
```

---

## Task 1: Setup do Monorepo (Raiz + Docker)

**Files:**
- Create: `package.json`
- Create: `turbo.json`
- Create: `docker-compose.yml`
- Create: `.env.example`
- Create: `.gitignore`

- [ ] **Passo 1: Criar `package.json` raiz**

```json
{
  "name": "crm",
  "private": true,
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "db:up": "docker compose up -d",
    "db:down": "docker compose down",
    "db:migrate": "turbo run db:migrate --filter=@crm/db",
    "db:seed": "turbo run db:seed --filter=@crm/db"
  },
  "devDependencies": {
    "turbo": "^2.0.0"
  }
}
```

- [ ] **Passo 2: Criar `turbo.json`**

```json
{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**", ".next/**"]
    },
    "dev": {
      "cache": false,
      "persistent": true
    },
    "db:migrate": { "cache": false },
    "db:seed": { "cache": false }
  }
}
```

- [ ] **Passo 3: Criar `docker-compose.yml`**

```yaml
services:
  db:
    image: postgres:16-alpine
    restart: unless-stopped
    ports:
      - "5432:5432"
    environment:
      POSTGRES_DB: crm_dev
      POSTGRES_USER: crm
      POSTGRES_PASSWORD: crm
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
```

- [ ] **Passo 4: Criar `.env.example`**

```
# Database
DATABASE_URL=postgresql://crm:crm@localhost:5432/crm_dev

# JWT (troque em produção)
JWT_ACCESS_SECRET=dev-access-secret-32chars-minimum
JWT_REFRESH_SECRET=dev-refresh-secret-32chars-minimum

# Portas
API_PORT=3001
WEB_URL=http://localhost:3000
NEXT_PUBLIC_API_URL=http://localhost:3000
API_URL=http://localhost:3001
```

- [ ] **Passo 5: Criar `.gitignore`**

```
node_modules/
.env
.env.local
dist/
.next/
.turbo/
.superpowers/
*.log
```

- [ ] **Passo 6: Instalar dependências raiz**

```bash
npm install
```

Esperado: `turbo` instalado em `node_modules/.bin/turbo`.

- [ ] **Passo 7: Criar estrutura de diretórios**

```bash
mkdir -p apps/api/src/lib apps/api/src/middleware apps/api/src/controllers apps/api/src/routes
mkdir -p apps/web/app/\(auth\)/login apps/web/app/\(protected\)/dashboard
mkdir -p apps/web/components/dashboard apps/web/lib
mkdir -p packages/shared/src/types
mkdir -p packages/db/src packages/db/prisma
```

- [ ] **Passo 8: Commit**

```bash
git add package.json turbo.json docker-compose.yml .env.example .gitignore
git commit -m "chore: monorepo root setup with Turborepo and Docker"
```

---

## Task 2: packages/shared — Tipos TypeScript

**Files:**
- Create: `packages/shared/package.json`
- Create: `packages/shared/tsconfig.json`
- Create: `packages/shared/src/types/user.ts`
- Create: `packages/shared/src/types/auth.ts`
- Create: `packages/shared/src/index.ts`

- [ ] **Passo 1: Criar `packages/shared/package.json`**

```json
{
  "name": "@crm/shared",
  "version": "0.0.0",
  "private": true,
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  }
}
```

- [ ] **Passo 2: Criar `packages/shared/tsconfig.json`**

```json
{
  "compilerOptions": {
    "target": "ES2020",
    "module": "CommonJS",
    "strict": true,
    "esModuleInterop": true,
    "skipLibCheck": true,
    "declaration": true,
    "outDir": "dist"
  },
  "include": ["src"]
}
```

- [ ] **Passo 3: Criar `packages/shared/src/types/user.ts`**

```typescript
export enum Role {
  ADMIN = 'ADMIN',
  USER = 'USER',
}

export interface User {
  id: string
  name: string
  email: string
  role: Role
  createdAt: string
  updatedAt: string
}
```

- [ ] **Passo 4: Criar `packages/shared/src/types/auth.ts`**

```typescript
import type { User } from './user'

export interface LoginRequest {
  email: string
  password: string
}

export interface MeResponse {
  user: User
}

export interface AuthError {
  error: string
}
```

- [ ] **Passo 5: Criar `packages/shared/src/index.ts`**

```typescript
export * from './types/user'
export * from './types/auth'
```

- [ ] **Passo 6: Instalar dependências do pacote**

```bash
cd packages/shared && npm install
```

- [ ] **Passo 7: Verificar tipos**

```bash
cd packages/shared && npx tsc --noEmit
```

Esperado: sem erros.

- [ ] **Passo 8: Commit**

```bash
git add packages/shared/
git commit -m "feat: add shared TypeScript types package"
```

---

## Task 3: packages/db — Prisma + Schema + Seed

**Files:**
- Create: `packages/db/package.json`
- Create: `packages/db/tsconfig.json`
- Create: `packages/db/prisma/schema.prisma`
- Create: `packages/db/src/index.ts`
- Create: `packages/db/prisma/seed.ts`
- Create: `packages/db/.env`

- [ ] **Passo 1: Criar `packages/db/package.json`**

```json
{
  "name": "@crm/db",
  "version": "0.0.0",
  "private": true,
  "main": "src/index.ts",
  "scripts": {
    "db:generate": "prisma generate",
    "db:migrate": "prisma migrate dev",
    "db:seed": "tsx prisma/seed.ts"
  },
  "dependencies": {
    "@prisma/client": "^5.14.0"
  },
  "devDependencies": {
    "bcrypt": "^5.1.1",
    "@types/bcrypt": "^5.0.2",
    "prisma": "^5.14.0",
    "tsx": "^4.11.0",
    "typescript": "^5.4.0"
  }
}
```

- [ ] **Passo 2: Criar `packages/db/tsconfig.json`**

```json
{
  "compilerOptions": {
    "target": "ES2020",
    "module": "CommonJS",
    "strict": true,
    "esModuleInterop": true,
    "skipLibCheck": true,
    "outDir": "dist"
  },
  "include": ["src", "prisma"]
}
```

- [ ] **Passo 3: Criar `packages/db/.env`**

```
DATABASE_URL=postgresql://crm:crm@localhost:5432/crm_dev
```

- [ ] **Passo 4: Criar `packages/db/prisma/schema.prisma`**

```prisma
generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model User {
  id           String   @id @default(cuid())
  name         String
  email        String   @unique
  passwordHash String
  role         Role     @default(USER)
  refreshToken String?
  createdAt    DateTime @default(now())
  updatedAt    DateTime @updatedAt
}

enum Role {
  ADMIN
  USER
}
```

- [ ] **Passo 5: Criar `packages/db/src/index.ts`**

```typescript
import { PrismaClient } from '@prisma/client'

const globalForPrisma = globalThis as unknown as { prisma: PrismaClient }

export const prisma =
  globalForPrisma.prisma ??
  new PrismaClient({
    log: process.env.NODE_ENV === 'development' ? ['error', 'warn'] : ['error'],
  })

if (process.env.NODE_ENV !== 'production') globalForPrisma.prisma = prisma

export { PrismaClient }
export * from '@prisma/client'
```

- [ ] **Passo 6: Criar `packages/db/prisma/seed.ts`**

```typescript
import { PrismaClient } from '@prisma/client'
import bcrypt from 'bcrypt'

const prisma = new PrismaClient()

async function main() {
  const passwordHash = await bcrypt.hash('admin123', 10)

  const admin = await prisma.user.upsert({
    where: { email: 'admin@crm.dev' },
    update: {},
    create: {
      name: 'Admin',
      email: 'admin@crm.dev',
      passwordHash,
      role: 'ADMIN',
    },
  })

  console.log(`Seed completo. Admin criado: ${admin.email}`)
}

main()
  .catch((e) => {
    console.error(e)
    process.exit(1)
  })
  .finally(() => prisma.$disconnect())
```

- [ ] **Passo 7: Instalar dependências**

```bash
cd packages/db && npm install
```

- [ ] **Passo 8: Subir o PostgreSQL**

```bash
cd ../.. && npm run db:up
```

Aguardar ~5 segundos. Verificar:

```bash
docker ps
```

Esperado: container `crm-db-1` com status `Up`.

- [ ] **Passo 9: Gerar client Prisma e rodar migration**

```bash
cd packages/db
npx prisma generate
npx prisma migrate dev --name init
```

Esperado: `migrations/XXXXXX_init/migration.sql` criada e aplicada.

- [ ] **Passo 10: Rodar o seed**

```bash
npm run db:seed
```

Esperado: `Seed completo. Admin criado: admin@crm.dev`

- [ ] **Passo 11: Verificar no banco**

```bash
docker exec -it crm-db-1 psql -U crm -d crm_dev -c "SELECT id, email, role FROM \"User\";"
```

Esperado: uma linha com `email = admin@crm.dev` e `role = ADMIN`.

- [ ] **Passo 12: Commit**

```bash
cd ../..
git add packages/db/
git commit -m "feat: add db package with Prisma schema and admin seed"
```

---

## Task 4: apps/api — Express Base

**Files:**
- Create: `apps/api/package.json`
- Create: `apps/api/tsconfig.json`
- Create: `apps/api/.env`
- Create: `apps/api/src/app.ts`
- Create: `apps/api/src/index.ts`

- [ ] **Passo 1: Criar `apps/api/package.json`**

```json
{
  "name": "@crm/api",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@crm/db": "*",
    "@crm/shared": "*",
    "bcrypt": "^5.1.1",
    "cookie-parser": "^1.4.7",
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "jsonwebtoken": "^9.0.2"
  },
  "devDependencies": {
    "@types/bcrypt": "^5.0.2",
    "@types/cookie-parser": "^1.4.7",
    "@types/cors": "^2.8.17",
    "@types/express": "^4.17.21",
    "@types/jsonwebtoken": "^9.0.6",
    "@types/node": "^20.0.0",
    "tsx": "^4.11.0",
    "typescript": "^5.4.0"
  }
}
```

- [ ] **Passo 2: Criar `apps/api/tsconfig.json`**

```json
{
  "compilerOptions": {
    "target": "ES2020",
    "module": "CommonJS",
    "strict": true,
    "esModuleInterop": true,
    "skipLibCheck": true,
    "outDir": "dist",
    "rootDir": "src",
    "paths": {
      "@crm/db": ["../../packages/db/src/index.ts"],
      "@crm/shared": ["../../packages/shared/src/index.ts"]
    }
  },
  "include": ["src"],
  "exclude": ["node_modules", "dist"]
}
```

- [ ] **Passo 3: Criar `apps/api/.env`**

```
DATABASE_URL=postgresql://crm:crm@localhost:5432/crm_dev
JWT_ACCESS_SECRET=dev-access-secret-change-in-production-32c
JWT_REFRESH_SECRET=dev-refresh-secret-change-in-production-32c
PORT=3001
WEB_URL=http://localhost:3000
NODE_ENV=development
```

- [ ] **Passo 4: Criar `apps/api/src/app.ts`**

```typescript
import express from 'express'
import cookieParser from 'cookie-parser'
import cors from 'cors'

const app = express()

app.use(
  cors({
    origin: process.env.WEB_URL || 'http://localhost:3000',
    credentials: true,
  })
)
app.use(express.json())
app.use(cookieParser())

app.get('/api/health', (_req, res) => {
  res.json({ ok: true, timestamp: new Date().toISOString() })
})

export default app
```

- [ ] **Passo 5: Criar `apps/api/src/index.ts`**

```typescript
import 'dotenv/config'
import app from './app'

const PORT = Number(process.env.PORT) || 3001

app.listen(PORT, () => {
  console.log(`API rodando em http://localhost:${PORT}`)
})
```

Nota: instalar `dotenv` junto com as dependências no próximo passo.

- [ ] **Passo 6: Adicionar `dotenv` ao `package.json` e instalar**

Abra `apps/api/package.json` e adicione `"dotenv": "^16.4.0"` em `dependencies`. Depois:

```bash
cd apps/api && npm install
```

- [ ] **Passo 7: Verificar health check**

Com o banco já rodando (Task 3), inicie a API:

```bash
npm run dev
```

Em outro terminal:

```bash
curl http://localhost:3001/api/health
```

Esperado: `{"ok":true,"timestamp":"..."}`

- [ ] **Passo 8: Commit**

```bash
git add apps/api/
git commit -m "feat: add Express API with health check endpoint"
```

---

## Task 5: apps/api — Autenticação

**Files:**
- Create: `apps/api/src/lib/jwt.ts`
- Create: `apps/api/src/lib/cookie.ts`
- Create: `apps/api/src/middleware/authenticate.ts`
- Create: `apps/api/src/controllers/auth.controller.ts`
- Create: `apps/api/src/routes/auth.ts`
- Modify: `apps/api/src/app.ts` (adicionar rota `/api/auth`)

- [ ] **Passo 1: Criar `apps/api/src/lib/jwt.ts`**

```typescript
import jwt from 'jsonwebtoken'

const ACCESS_SECRET = process.env.JWT_ACCESS_SECRET!
const REFRESH_SECRET = process.env.JWT_REFRESH_SECRET!

export function signAccessToken(userId: string): string {
  return jwt.sign({ sub: userId }, ACCESS_SECRET, { expiresIn: '15m' })
}

export function signRefreshToken(userId: string): string {
  return jwt.sign({ sub: userId }, REFRESH_SECRET, { expiresIn: '7d' })
}

export function verifyAccessToken(token: string): { sub: string } {
  return jwt.verify(token, ACCESS_SECRET) as { sub: string }
}

export function verifyRefreshToken(token: string): { sub: string } {
  return jwt.verify(token, REFRESH_SECRET) as { sub: string }
}
```

- [ ] **Passo 2: Criar `apps/api/src/lib/cookie.ts`**

```typescript
import type { Response } from 'express'

const IS_PROD = process.env.NODE_ENV === 'production'

export function setAuthCookies(
  res: Response,
  accessToken: string,
  refreshToken: string
) {
  res.cookie('access_token', accessToken, {
    httpOnly: true,
    secure: IS_PROD,
    sameSite: 'lax',
    maxAge: 15 * 60 * 1000,
  })
  res.cookie('refresh_token', refreshToken, {
    httpOnly: true,
    secure: IS_PROD,
    sameSite: 'lax',
    maxAge: 7 * 24 * 60 * 60 * 1000,
    path: '/api/auth/refresh',
  })
}

export function clearAuthCookies(res: Response) {
  res.clearCookie('access_token')
  res.clearCookie('refresh_token', { path: '/api/auth/refresh' })
}
```

- [ ] **Passo 3: Criar `apps/api/src/middleware/authenticate.ts`**

```typescript
import type { Request, Response, NextFunction } from 'express'
import { verifyAccessToken } from '../lib/jwt'

export function authenticate(req: Request, res: Response, next: NextFunction) {
  const token: string | undefined = req.cookies.access_token
  if (!token) {
    res.status(401).json({ error: 'Unauthorized' })
    return
  }
  try {
    const payload = verifyAccessToken(token)
    res.locals.userId = payload.sub
    next()
  } catch {
    res.status(401).json({ error: 'Token inválido ou expirado' })
  }
}
```

- [ ] **Passo 4: Criar `apps/api/src/controllers/auth.controller.ts`**

```typescript
import type { Request, Response } from 'express'
import bcrypt from 'bcrypt'
import { prisma } from '@crm/db'
import { signAccessToken, signRefreshToken, verifyRefreshToken } from '../lib/jwt'
import { setAuthCookies, clearAuthCookies } from '../lib/cookie'

export async function login(req: Request, res: Response) {
  const { email, password } = req.body as { email?: string; password?: string }

  if (!email || !password) {
    res.status(400).json({ error: 'E-mail e senha são obrigatórios' })
    return
  }

  const user = await prisma.user.findUnique({ where: { email } })
  if (!user) {
    res.status(401).json({ error: 'Credenciais inválidas' })
    return
  }

  const valid = await bcrypt.compare(password, user.passwordHash)
  if (!valid) {
    res.status(401).json({ error: 'Credenciais inválidas' })
    return
  }

  const accessToken = signAccessToken(user.id)
  const refreshToken = signRefreshToken(user.id)

  await prisma.user.update({
    where: { id: user.id },
    data: { refreshToken },
  })

  setAuthCookies(res, accessToken, refreshToken)

  res.json({
    user: {
      id: user.id,
      name: user.name,
      email: user.email,
      role: user.role,
    },
  })
}

export async function logout(req: Request, res: Response) {
  const userId = res.locals.userId as string

  await prisma.user.update({
    where: { id: userId },
    data: { refreshToken: null },
  })

  clearAuthCookies(res)
  res.json({ ok: true })
}

export async function refresh(req: Request, res: Response) {
  const token: string | undefined = req.cookies.refresh_token
  if (!token) {
    res.status(401).json({ error: 'Sem refresh token' })
    return
  }

  try {
    const { sub } = verifyRefreshToken(token)
    const user = await prisma.user.findUnique({ where: { id: sub } })

    if (!user || user.refreshToken !== token) {
      res.status(401).json({ error: 'Refresh token inválido' })
      return
    }

    const newAccessToken = signAccessToken(user.id)
    const newRefreshToken = signRefreshToken(user.id)

    await prisma.user.update({
      where: { id: user.id },
      data: { refreshToken: newRefreshToken },
    })

    setAuthCookies(res, newAccessToken, newRefreshToken)
    res.json({ ok: true })
  } catch {
    res.status(401).json({ error: 'Refresh token inválido' })
  }
}

export async function me(req: Request, res: Response) {
  const userId = res.locals.userId as string

  const user = await prisma.user.findUnique({
    where: { id: userId },
    select: {
      id: true,
      name: true,
      email: true,
      role: true,
      createdAt: true,
      updatedAt: true,
    },
  })

  if (!user) {
    res.status(404).json({ error: 'Usuário não encontrado' })
    return
  }

  res.json({ user })
}
```

- [ ] **Passo 5: Criar `apps/api/src/routes/auth.ts`**

```typescript
import { Router } from 'express'
import { login, logout, refresh, me } from '../controllers/auth.controller'
import { authenticate } from '../middleware/authenticate'

const router = Router()

router.post('/login', login)
router.post('/logout', authenticate, logout)
router.post('/refresh', refresh)
router.get('/me', authenticate, me)

export default router
```

- [ ] **Passo 6: Registrar a rota em `apps/api/src/app.ts`**

Substitua o conteúdo de `apps/api/src/app.ts` por:

```typescript
import express from 'express'
import cookieParser from 'cookie-parser'
import cors from 'cors'
import authRouter from './routes/auth'

const app = express()

app.use(
  cors({
    origin: process.env.WEB_URL || 'http://localhost:3000',
    credentials: true,
  })
)
app.use(express.json())
app.use(cookieParser())

app.use('/api/auth', authRouter)

app.get('/api/health', (_req, res) => {
  res.json({ ok: true, timestamp: new Date().toISOString() })
})

export default app
```

- [ ] **Passo 7: Verificar login via curl**

Com a API rodando (`npm run dev` em `apps/api/`):

```bash
curl -s -c cookies.txt -X POST http://localhost:3001/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"email":"admin@crm.dev","password":"admin123"}' | cat
```

Esperado: `{"user":{"id":"...","name":"Admin","email":"admin@crm.dev","role":"ADMIN"}}`

- [ ] **Passo 8: Verificar /me**

```bash
curl -s -b cookies.txt http://localhost:3001/api/auth/me | cat
```

Esperado: `{"user":{"id":"...","name":"Admin",...}}`

- [ ] **Passo 9: Verificar que rota protegida sem token retorna 401**

```bash
curl -s http://localhost:3001/api/auth/me | cat
```

Esperado: `{"error":"Unauthorized"}`

- [ ] **Passo 10: Commit**

```bash
git add apps/api/src/
git commit -m "feat: add JWT auth (login, logout, refresh, me) with httpOnly cookies"
```

---

## Task 6: apps/web — Setup Next.js

**Files:**
- Create: `apps/web/package.json`
- Create: `apps/web/tsconfig.json`
- Create: `apps/web/next.config.ts`
- Create: `apps/web/tailwind.config.ts`
- Create: `apps/web/postcss.config.js`
- Create: `apps/web/.env.local`
- Create: `apps/web/app/globals.css`
- Create: `apps/web/app/layout.tsx`
- Create: `apps/web/middleware.ts`
- Create: `apps/web/lib/api-client.ts`

- [ ] **Passo 1: Criar `apps/web/package.json`**

```json
{
  "name": "@crm/web",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "next dev -p 3000",
    "build": "next build",
    "start": "next start -p 3000"
  },
  "dependencies": {
    "@crm/shared": "*",
    "next": "14.2.3",
    "react": "^18.3.0",
    "react-dom": "^18.3.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/react": "^18.3.0",
    "@types/react-dom": "^18.3.0",
    "autoprefixer": "^10.4.19",
    "postcss": "^8.4.38",
    "tailwindcss": "^3.4.4",
    "typescript": "^5.4.0"
  }
}
```

- [ ] **Passo 2: Criar `apps/web/tsconfig.json`**

```json
{
  "compilerOptions": {
    "target": "ES2017",
    "lib": ["dom", "dom.iterable", "esnext"],
    "allowJs": true,
    "skipLibCheck": true,
    "strict": true,
    "noEmit": true,
    "esModuleInterop": true,
    "module": "esnext",
    "moduleResolution": "bundler",
    "resolveJsonModule": true,
    "isolatedModules": true,
    "jsx": "preserve",
    "incremental": true,
    "plugins": [{ "name": "next" }],
    "paths": {
      "@/*": ["./*"],
      "@crm/shared": ["../../packages/shared/src/index.ts"]
    }
  },
  "include": ["next-env.d.ts", "**/*.ts", "**/*.tsx", ".next/types/**/*.ts"],
  "exclude": ["node_modules"]
}
```

- [ ] **Passo 3: Criar `apps/web/next.config.ts`**

```typescript
import type { NextConfig } from 'next'

const nextConfig: NextConfig = {
  transpilePackages: ['@crm/shared'],
  async rewrites() {
    return [
      {
        source: '/api/:path*',
        destination: `${process.env.API_URL || 'http://localhost:3001'}/api/:path*`,
      },
    ]
  },
}

export default nextConfig
```

- [ ] **Passo 4: Criar `apps/web/tailwind.config.ts`**

```typescript
import type { Config } from 'tailwindcss'

const config: Config = {
  content: [
    './app/**/*.{ts,tsx}',
    './components/**/*.{ts,tsx}',
    './lib/**/*.{ts,tsx}',
  ],
  theme: {
    extend: {},
  },
  plugins: [],
}

export default config
```

- [ ] **Passo 5: Criar `apps/web/postcss.config.js`**

```javascript
module.exports = {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
}
```

- [ ] **Passo 6: Criar `apps/web/.env.local`**

```
API_URL=http://localhost:3001
NEXT_PUBLIC_API_URL=http://localhost:3000
```

(O `NEXT_PUBLIC_API_URL` aponta para localhost:3000 porque o Next.js faz proxy das requisições `/api/*`.)

- [ ] **Passo 7: Criar `apps/web/app/globals.css`**

```css
@tailwind base;
@tailwind components;
@tailwind utilities;

* {
  box-sizing: border-box;
}

html,
body {
  height: 100%;
}
```

- [ ] **Passo 8: Criar `apps/web/app/layout.tsx`**

```tsx
import type { Metadata } from 'next'
import './globals.css'

export const metadata: Metadata = {
  title: 'MyCRM',
  description: 'CRM para equipes pequenas',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="pt-BR">
      <body className="bg-slate-50 text-slate-900 antialiased">{children}</body>
    </html>
  )
}
```

- [ ] **Passo 9: Criar `apps/web/middleware.ts`**

```typescript
import { NextResponse } from 'next/server'
import type { NextRequest } from 'next/server'

export function middleware(req: NextRequest) {
  const token = req.cookies.get('access_token')?.value
  const isLoginPage = req.nextUrl.pathname === '/login'

  if (!token && !isLoginPage) {
    return NextResponse.redirect(new URL('/login', req.url))
  }
  if (token && isLoginPage) {
    return NextResponse.redirect(new URL('/dashboard', req.url))
  }

  return NextResponse.next()
}

export const config = {
  matcher: ['/((?!_next/static|_next/image|favicon.ico).*)'],
}
```

- [ ] **Passo 10: Criar `apps/web/lib/api-client.ts`**

```typescript
const BASE = process.env.NEXT_PUBLIC_API_URL || ''

export async function apiFetch<T>(
  path: string,
  options?: RequestInit
): Promise<T> {
  const res = await fetch(`${BASE}${path}`, {
    credentials: 'include',
    headers: { 'Content-Type': 'application/json', ...options?.headers },
    ...options,
  })

  if (!res.ok) {
    const body = await res.json().catch(() => ({ error: 'Erro desconhecido' }))
    throw new Error(body.error ?? `HTTP ${res.status}`)
  }

  return res.json() as Promise<T>
}
```

- [ ] **Passo 11: Instalar dependências**

```bash
cd apps/web && npm install
```

- [ ] **Passo 12: Verificar que o Next.js inicia**

```bash
npm run dev
```

Abra http://localhost:3000 no browser. Esperado: redirecionar para `/login` (página ainda não existe, erro 404 é normal por ora).

- [ ] **Passo 13: Commit**

```bash
git add apps/web/
git commit -m "feat: add Next.js app with Tailwind, API proxy, and route protection middleware"
```

---

## Task 7: apps/web — Página de Login

**Files:**
- Create: `apps/web/app/(auth)/login/page.tsx`

- [ ] **Passo 1: Criar `apps/web/app/(auth)/login/page.tsx`**

```tsx
'use client'

import { useState, FormEvent } from 'react'
import { useRouter } from 'next/navigation'
import { apiFetch } from '@/lib/api-client'
import type { MeResponse } from '@crm/shared'

export default function LoginPage() {
  const router = useRouter()
  const [error, setError] = useState('')
  const [loading, setLoading] = useState(false)

  async function handleSubmit(e: FormEvent<HTMLFormElement>) {
    e.preventDefault()
    setLoading(true)
    setError('')

    const form = new FormData(e.currentTarget)

    try {
      await apiFetch<MeResponse>('/api/auth/login', {
        method: 'POST',
        body: JSON.stringify({
          email: form.get('email'),
          password: form.get('password'),
        }),
      })
      router.push('/dashboard')
      router.refresh()
    } catch (err) {
      setError(err instanceof Error ? err.message : 'Erro ao fazer login')
      setLoading(false)
    }
  }

  return (
    <div className="min-h-screen flex items-center justify-center bg-slate-900">
      <div className="w-full max-w-md">
        <div className="text-center mb-8">
          <h1 className="text-3xl font-bold text-white">
            My<span className="text-indigo-400">CRM</span>
          </h1>
          <p className="text-slate-400 mt-2 text-sm">Faça login para continuar</p>
        </div>

        <form
          onSubmit={handleSubmit}
          className="bg-white rounded-2xl shadow-xl p-8 space-y-5"
        >
          {error && (
            <div className="bg-red-50 border border-red-200 text-red-700 text-sm rounded-lg px-4 py-3">
              {error}
            </div>
          )}

          <div>
            <label
              htmlFor="email"
              className="block text-sm font-medium text-slate-700 mb-1.5"
            >
              E-mail
            </label>
            <input
              id="email"
              name="email"
              type="email"
              required
              autoComplete="email"
              placeholder="admin@crm.dev"
              className="w-full px-4 py-2.5 border border-slate-200 rounded-lg text-sm focus:outline-none focus:ring-2 focus:ring-indigo-500 focus:border-transparent"
            />
          </div>

          <div>
            <label
              htmlFor="password"
              className="block text-sm font-medium text-slate-700 mb-1.5"
            >
              Senha
            </label>
            <input
              id="password"
              name="password"
              type="password"
              required
              autoComplete="current-password"
              placeholder="••••••••"
              className="w-full px-4 py-2.5 border border-slate-200 rounded-lg text-sm focus:outline-none focus:ring-2 focus:ring-indigo-500 focus:border-transparent"
            />
          </div>

          <button
            type="submit"
            disabled={loading}
            className="w-full bg-indigo-600 text-white py-2.5 rounded-lg text-sm font-semibold hover:bg-indigo-700 disabled:opacity-60 disabled:cursor-not-allowed transition-colors"
          >
            {loading ? 'Entrando...' : 'Entrar'}
          </button>
        </form>
      </div>
    </div>
  )
}
```

- [ ] **Passo 2: Verificar a página de login**

Com `apps/web` e `apps/api` rodando, abra http://localhost:3000/login.

Esperado: formulário de login aparece com campo e-mail, senha e botão "Entrar".

- [ ] **Passo 3: Testar login com credenciais corretas**

Preencha: `admin@crm.dev` / `admin123`. Clique Entrar.

Esperado: redireciona para `/dashboard` (página 404 por enquanto — isso é normal).

- [ ] **Passo 4: Testar login com credenciais erradas**

Volte para `/login`, tente `admin@crm.dev` / `senha-errada`.

Esperado: mensagem de erro `Credenciais inválidas` aparece em vermelho.

- [ ] **Passo 5: Commit**

```bash
git add apps/web/app/\(auth\)/
git commit -m "feat: add login page with form validation and error handling"
```

---

## Task 8: apps/web — Layout Protegido + Sidebar + Topbar

**Files:**
- Create: `apps/web/app/(protected)/layout.tsx`
- Create: `apps/web/components/sidebar.tsx`
- Create: `apps/web/components/topbar.tsx`

- [ ] **Passo 1: Criar `apps/web/components/sidebar.tsx`**

```tsx
'use client'

import Link from 'next/link'
import { usePathname } from 'next/navigation'
import { apiFetch } from '@/lib/api-client'
import { useRouter } from 'next/navigation'

interface NavItem {
  label: string
  href: string
  icon: string
  phase: number | null
}

const NAV_ITEMS: NavItem[] = [
  { label: 'Dashboard', href: '/dashboard', icon: '📊', phase: null },
  { label: 'Contatos', href: '/contatos', icon: '👥', phase: 2 },
  { label: 'Pipeline', href: '/pipeline', icon: '💼', phase: 3 },
  { label: 'Tarefas', href: '/tarefas', icon: '✅', phase: 4 },
  { label: 'Suporte', href: '/suporte', icon: '🎧', phase: 5 },
  { label: 'Marketing', href: '/marketing', icon: '📣', phase: 6 },
]

interface SidebarProps {
  user: { name: string; email: string; role: string }
}

export default function Sidebar({ user }: SidebarProps) {
  const pathname = usePathname()
  const router = useRouter()

  const initials = user.name
    .split(' ')
    .map((n) => n[0])
    .slice(0, 2)
    .join('')
    .toUpperCase()

  async function handleLogout() {
    try {
      await apiFetch('/api/auth/logout', { method: 'POST' })
    } finally {
      router.push('/login')
      router.refresh()
    }
  }

  return (
    <aside className="w-52 bg-slate-900 flex flex-col flex-shrink-0 h-screen">
      {/* Logo */}
      <div className="px-4 py-4 border-b border-slate-800">
        <span className="text-white font-bold text-lg tracking-tight">
          My<span className="text-indigo-400">CRM</span>
        </span>
      </div>

      {/* Nav */}
      <nav className="flex-1 py-2">
        <p className="px-4 py-2 text-[10px] font-semibold text-slate-500 uppercase tracking-widest">
          Principal
        </p>
        {NAV_ITEMS.slice(0, 4).map((item) => (
          <NavLink key={item.href} item={item} pathname={pathname} />
        ))}

        <p className="px-4 py-2 mt-2 text-[10px] font-semibold text-slate-500 uppercase tracking-widest">
          Outros
        </p>
        {NAV_ITEMS.slice(4).map((item) => (
          <NavLink key={item.href} item={item} pathname={pathname} />
        ))}
      </nav>

      {/* User */}
      <div className="border-t border-slate-800 p-3 flex items-center gap-2.5">
        <div className="w-7 h-7 rounded-full bg-indigo-600 flex items-center justify-center text-white text-[11px] font-bold flex-shrink-0">
          {initials}
        </div>
        <div className="flex-1 overflow-hidden">
          <p className="text-white text-xs font-medium truncate">{user.name}</p>
          <p className="text-slate-500 text-[10px]">{user.role}</p>
        </div>
        <button
          onClick={handleLogout}
          title="Sair"
          className="text-slate-500 hover:text-white text-xs transition-colors"
        >
          ↩
        </button>
      </div>
    </aside>
  )
}

function NavLink({ item, pathname }: { item: NavItem; pathname: string }) {
  const isActive = pathname === item.href
  const isDisabled = item.phase !== null

  if (isDisabled) {
    return (
      <div className="flex items-center gap-2.5 px-4 py-2 text-slate-700 cursor-default select-none">
        <span className="text-sm w-4 text-center">{item.icon}</span>
        <span className="text-[13px]">{item.label}</span>
        <span className="ml-auto text-[9px] bg-slate-800 text-slate-500 px-1.5 py-0.5 rounded-full">
          Fase {item.phase}
        </span>
      </div>
    )
  }

  return (
    <Link
      href={item.href}
      className={`flex items-center gap-2.5 px-4 py-2 text-[13px] transition-colors relative ${
        isActive
          ? 'bg-slate-800 text-white'
          : 'text-slate-400 hover:text-white hover:bg-slate-800/50'
      }`}
    >
      {isActive && (
        <span className="absolute left-0 top-0 bottom-0 w-[3px] bg-indigo-500 rounded-r" />
      )}
      <span className="text-sm w-4 text-center">{item.icon}</span>
      {item.label}
    </Link>
  )
}
```

- [ ] **Passo 2: Criar `apps/web/components/topbar.tsx`**

```tsx
export default function Topbar() {
  const today = new Date().toLocaleDateString('pt-BR', {
    weekday: 'long',
    day: 'numeric',
    month: 'long',
    year: 'numeric',
  })

  const pageTitle = 'Dashboard'

  return (
    <header className="bg-white border-b border-slate-200 px-5 py-2.5 flex items-center justify-between flex-shrink-0">
      <div>
        <h1 className="text-base font-semibold text-slate-900">{pageTitle}</h1>
        <p className="text-xs text-slate-400 capitalize">{today}</p>
      </div>
      <div className="flex items-center gap-2">
        <button className="w-8 h-8 rounded-lg bg-slate-100 flex items-center justify-center text-sm hover:bg-slate-200 transition-colors">
          🔔
        </button>
        <button className="w-8 h-8 rounded-lg bg-slate-100 flex items-center justify-center text-sm hover:bg-slate-200 transition-colors">
          ⚙️
        </button>
      </div>
    </header>
  )
}
```

- [ ] **Passo 3: Criar `apps/web/app/(protected)/layout.tsx`**

```tsx
import { cookies } from 'next/headers'
import { redirect } from 'next/navigation'
import Sidebar from '@/components/sidebar'
import Topbar from '@/components/topbar'

async function getCurrentUser() {
  const cookieStore = await cookies()
  const accessToken = cookieStore.get('access_token')?.value

  if (!accessToken) redirect('/login')

  const res = await fetch(
    `${process.env.API_URL || 'http://localhost:3001'}/api/auth/me`,
    {
      headers: { Cookie: `access_token=${accessToken}` },
      cache: 'no-store',
    }
  )

  if (!res.ok) redirect('/login')

  const data = await res.json() as { user: { name: string; email: string; role: string } }
  return data.user
}

export default async function ProtectedLayout({
  children,
}: {
  children: React.ReactNode
}) {
  const user = await getCurrentUser()

  return (
    <div className="flex h-screen overflow-hidden">
      <Sidebar user={user} />
      <div className="flex flex-col flex-1 overflow-hidden">
        <Topbar />
        <main className="flex-1 overflow-auto bg-slate-50">{children}</main>
      </div>
    </div>
  )
}
```

- [ ] **Passo 4: Verificar layout**

Com `apps/api` e `apps/web` rodando, faça login em http://localhost:3000/login.

Esperado: redireciona para `/dashboard`, sidebar escura aparece à esquerda com os módulos, topbar branca no topo. Página principal ainda está vazia (404).

- [ ] **Passo 5: Commit**

```bash
git add apps/web/app/\(protected\)/ apps/web/components/
git commit -m "feat: add protected layout with sidebar and topbar"
```

---

## Task 9: apps/web — Dashboard com Dados Mock

**Files:**
- Create: `apps/web/lib/mock-data.ts`
- Create: `apps/web/components/dashboard/kpi-card.tsx`
- Create: `apps/web/components/dashboard/funnel-chart.tsx`
- Create: `apps/web/components/dashboard/activity-feed.tsx`
- Create: `apps/web/components/dashboard/deals-list.tsx`
- Create: `apps/web/components/dashboard/upcoming-tasks.tsx`
- Create: `apps/web/app/(protected)/dashboard/page.tsx`

- [ ] **Passo 1: Criar `apps/web/lib/mock-data.ts`**

```typescript
export const MOCK_KPIS = [
  { id: 'contacts', label: 'Total de Contatos', value: '1.284', change: '↑ 12% este mês', trend: 'up' as const, icon: '👥' },
  { id: 'deals', label: 'Negociações Abertas', value: '47', change: '↑ 5 esta semana', trend: 'up' as const, icon: '💼' },
  { id: 'revenue', label: 'Receita Prevista', value: 'R$ 218k', change: '↓ 3% vs mês anterior', trend: 'down' as const, icon: '💰' },
  { id: 'tickets', label: 'Tickets em Aberto', value: '23', change: '↑ 2 hoje', trend: 'up' as const, icon: '🎧' },
]

export const MOCK_FUNNEL = [
  { stage: 'Prospecção', count: 142, pct: 100, color: '#6366f1' },
  { stage: 'Qualificação', count: 102, pct: 72, color: '#8b5cf6' },
  { stage: 'Proposta', count: 68, pct: 48, color: '#a78bfa' },
  { stage: 'Negociação', count: 47, pct: 33, color: '#c4b5fd' },
  { stage: 'Fechamento', count: 26, pct: 18, color: '#22c55e' },
]

export const MOCK_ACTIVITIES = [
  { id: '1', color: '#6366f1', text: 'Nova negociação aberta com Tech Solutions', time: 'há 12 minutos' },
  { id: '2', color: '#22c55e', text: 'Contato Maria Silva atualizado', time: 'há 38 minutos' },
  { id: '3', color: '#f59e0b', text: 'Ticket #304 resolvido por Carlos M.', time: 'há 1 hora' },
  { id: '4', color: '#ef4444', text: 'Negociação Projeto Alpha movida para Proposta', time: 'há 2 horas' },
  { id: '5', color: '#8b5cf6', text: 'Campanha E-mail Maio enviada — 340 destinatários', time: 'há 3 horas' },
]

export const MOCK_DEALS = [
  { id: '1', name: 'Projeto Alpha', company: 'Tech Solutions', value: 'R$ 48.000', status: 'Proposta', statusColor: 'bg-orange-100 text-orange-700' },
  { id: '2', name: 'Contrato Anual', company: 'GlobalCorp', value: 'R$ 120.000', status: 'Negociação', statusColor: 'bg-purple-100 text-purple-700' },
  { id: '3', name: 'Módulo Extra', company: 'StartupXYZ', value: 'R$ 18.500', status: 'Qualificação', statusColor: 'bg-blue-100 text-blue-700' },
  { id: '4', name: 'Renovação 2026', company: 'Mega Ind.', value: 'R$ 32.000', status: 'Fechamento', statusColor: 'bg-green-100 text-green-700' },
]

export const MOCK_TASKS = [
  { id: '1', title: 'Reunião — Tech Solutions', datetime: 'Hoje, 14h00', type: 'Reunião', typeColor: 'bg-blue-100 text-blue-700' },
  { id: '2', title: 'Follow-up — Maria Silva', datetime: 'Amanhã, 09h00', type: 'E-mail', typeColor: 'bg-orange-100 text-orange-700' },
  { id: '3', title: 'Demo — GlobalCorp', datetime: '02/06, 15h30', type: 'Demo', typeColor: 'bg-purple-100 text-purple-700' },
  { id: '4', title: 'Proposta — StartupXYZ', datetime: '03/06, 10h00', type: 'Tarefa', typeColor: 'bg-green-100 text-green-700' },
]
```

- [ ] **Passo 2: Criar `apps/web/components/dashboard/kpi-card.tsx`**

```tsx
interface KpiCardProps {
  label: string
  value: string
  change: string
  trend: 'up' | 'down'
  icon: string
}

export default function KpiCard({ label, value, change, trend, icon }: KpiCardProps) {
  return (
    <div className="bg-white rounded-xl border border-slate-200 p-4">
      <div className="flex items-start justify-between">
        <p className="text-xs font-medium text-slate-500 uppercase tracking-wide">{label}</p>
        <span className="text-xl opacity-20">{icon}</span>
      </div>
      <p className="text-2xl font-bold text-slate-900 mt-2 mb-1">{value}</p>
      <p className={`text-xs font-medium ${trend === 'up' ? 'text-green-600' : 'text-red-500'}`}>
        {change}
      </p>
    </div>
  )
}
```

- [ ] **Passo 3: Criar `apps/web/components/dashboard/funnel-chart.tsx`**

```tsx
interface FunnelRow {
  stage: string
  count: number
  pct: number
  color: string
}

interface FunnelChartProps {
  data: FunnelRow[]
}

export default function FunnelChart({ data }: FunnelChartProps) {
  return (
    <div className="bg-white rounded-xl border border-slate-200 p-4">
      <h3 className="text-sm font-semibold text-slate-900 mb-4">Funil de Vendas</h3>
      <div className="space-y-2.5">
        {data.map((row) => (
          <div key={row.stage} className="flex items-center gap-3">
            <span className="text-xs text-slate-500 w-24 text-right flex-shrink-0">
              {row.stage}
            </span>
            <div className="flex-1 bg-slate-100 rounded h-5 overflow-hidden">
              <div
                className="h-full rounded flex items-center px-2"
                style={{ width: `${row.pct}%`, backgroundColor: row.color }}
              >
                <span className="text-[10px] font-semibold text-white">{row.count}</span>
              </div>
            </div>
            <span className="text-xs text-slate-400 w-6 flex-shrink-0">{row.count}</span>
          </div>
        ))}
      </div>
    </div>
  )
}
```

- [ ] **Passo 4: Criar `apps/web/components/dashboard/activity-feed.tsx`**

```tsx
interface Activity {
  id: string
  color: string
  text: string
  time: string
}

interface ActivityFeedProps {
  activities: Activity[]
}

export default function ActivityFeed({ activities }: ActivityFeedProps) {
  return (
    <div className="bg-white rounded-xl border border-slate-200 p-4">
      <h3 className="text-sm font-semibold text-slate-900 mb-4">Atividades Recentes</h3>
      <div className="space-y-3">
        {activities.map((a) => (
          <div key={a.id} className="flex gap-3">
            <div
              className="w-2 h-2 rounded-full mt-1.5 flex-shrink-0"
              style={{ backgroundColor: a.color }}
            />
            <div>
              <p className="text-xs text-slate-700 leading-snug">{a.text}</p>
              <p className="text-[10px] text-slate-400 mt-0.5">{a.time}</p>
            </div>
          </div>
        ))}
      </div>
    </div>
  )
}
```

- [ ] **Passo 5: Criar `apps/web/components/dashboard/deals-list.tsx`**

```tsx
interface Deal {
  id: string
  name: string
  company: string
  value: string
  status: string
  statusColor: string
}

interface DealsListProps {
  deals: Deal[]
}

export default function DealsList({ deals }: DealsListProps) {
  return (
    <div className="bg-white rounded-xl border border-slate-200 p-4">
      <h3 className="text-sm font-semibold text-slate-900 mb-3">Negociações em Destaque</h3>
      <div className="divide-y divide-slate-100">
        {deals.map((deal) => (
          <div key={deal.id} className="py-2.5 flex items-center justify-between gap-2">
            <div className="overflow-hidden">
              <p className="text-xs font-medium text-slate-800 truncate">{deal.name}</p>
              <p className="text-[10px] text-slate-400">
                {deal.company} · {deal.value}
              </p>
            </div>
            <span
              className={`text-[10px] font-medium px-2 py-0.5 rounded-full flex-shrink-0 ${deal.statusColor}`}
            >
              {deal.status}
            </span>
          </div>
        ))}
      </div>
    </div>
  )
}
```

- [ ] **Passo 6: Criar `apps/web/components/dashboard/upcoming-tasks.tsx`**

```tsx
interface Task {
  id: string
  title: string
  datetime: string
  type: string
  typeColor: string
}

interface UpcomingTasksProps {
  tasks: Task[]
}

export default function UpcomingTasks({ tasks }: UpcomingTasksProps) {
  return (
    <div className="bg-white rounded-xl border border-slate-200 p-4">
      <h3 className="text-sm font-semibold text-slate-900 mb-3">Próximas Atividades</h3>
      <div className="divide-y divide-slate-100">
        {tasks.map((task) => (
          <div key={task.id} className="py-2.5 flex items-center justify-between gap-2">
            <div className="overflow-hidden">
              <p className="text-xs font-medium text-slate-800 truncate">{task.title}</p>
              <p className="text-[10px] text-slate-400">{task.datetime}</p>
            </div>
            <span
              className={`text-[10px] font-medium px-2 py-0.5 rounded-full flex-shrink-0 ${task.typeColor}`}
            >
              {task.type}
            </span>
          </div>
        ))}
      </div>
    </div>
  )
}
```

- [ ] **Passo 7: Criar `apps/web/app/(protected)/dashboard/page.tsx`**

```tsx
import KpiCard from '@/components/dashboard/kpi-card'
import FunnelChart from '@/components/dashboard/funnel-chart'
import ActivityFeed from '@/components/dashboard/activity-feed'
import DealsList from '@/components/dashboard/deals-list'
import UpcomingTasks from '@/components/dashboard/upcoming-tasks'
import {
  MOCK_KPIS,
  MOCK_FUNNEL,
  MOCK_ACTIVITIES,
  MOCK_DEALS,
  MOCK_TASKS,
} from '@/lib/mock-data'

export default function DashboardPage() {
  return (
    <div className="p-5 space-y-4">
      {/* Banner de dados mock */}
      <div className="bg-amber-50 border border-amber-200 rounded-lg px-4 py-2.5 text-xs text-amber-800">
        ⚠️ <strong>Dados de demonstração</strong> — os módulos reais serão ativados nas próximas fases.
      </div>

      {/* KPIs */}
      <div className="grid grid-cols-2 lg:grid-cols-4 gap-3">
        {MOCK_KPIS.map((kpi) => (
          <KpiCard key={kpi.id} {...kpi} />
        ))}
      </div>

      {/* Funil + Atividades */}
      <div className="grid grid-cols-1 lg:grid-cols-2 gap-4">
        <FunnelChart data={MOCK_FUNNEL} />
        <ActivityFeed activities={MOCK_ACTIVITIES} />
      </div>

      {/* Negociações + Tarefas */}
      <div className="grid grid-cols-1 lg:grid-cols-2 gap-4">
        <DealsList deals={MOCK_DEALS} />
        <UpcomingTasks tasks={MOCK_TASKS} />
      </div>
    </div>
  )
}
```

- [ ] **Passo 8: Verificar o dashboard completo**

Com `apps/api` e `apps/web` rodando, faça login e navegue para http://localhost:3000/dashboard.

Verificar:
- 4 cards de KPI no topo
- Funil de vendas e atividades recentes na linha do meio
- Negociações e próximas tarefas na linha de baixo
- Banner amarelo de dados mock
- Sidebar com módulos desabilitados
- Nome do usuário e botão de logout no rodapé da sidebar

- [ ] **Passo 9: Testar logout**

Clique no botão ↩ no rodapé da sidebar.

Esperado: redireciona para `/login`.

Tente acessar http://localhost:3000/dashboard diretamente.

Esperado: redireciona para `/login` (middleware funcionando).

- [ ] **Passo 10: Commit final**

```bash
git add apps/web/app/\(protected\)/dashboard/ apps/web/components/dashboard/ apps/web/lib/mock-data.ts
git commit -m "feat: add dashboard with mocked KPIs, funnel, activities, deals and tasks"
```

---

## Verificação Final

Após completar todas as tasks, rode o sistema completo com um único comando na raiz:

```bash
npm run db:up && npm run dev
```

Checklist de aceitação:
- [ ] http://localhost:3000 redireciona para `/login`
- [ ] Login com `admin@crm.dev` / `admin123` funciona
- [ ] Dashboard carrega com todos os blocos mockados
- [ ] Sidebar mostra módulos futuros com badge "Fase N"
- [ ] Logout redireciona para `/login` e bloqueia acesso ao dashboard
- [ ] http://localhost:3001/api/health retorna `{"ok":true,...}`
- [ ] Cookies `access_token` aparecem como httpOnly nas DevTools do browser
